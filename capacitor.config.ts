import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'campeonato.mobile',
  appName: 'campeonato-mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
